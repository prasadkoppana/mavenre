@Library('libary')_

pipeline
{
     agent any
     stages
     {
         stage('call the function ')
         {
             steps
             {
                 script
                 {
                    math.sum(10,20) 
                    math.mul(20,5)
                    math.sub(10,30)
                 }
             }
             
         }
         
     }
}
